# Build verification

This branch exists only to trigger a pull-request Android build so the APK artifact and compiler logs can be inspected through the connected GitHub tooling.

Verification trigger: 2026-08-23.
